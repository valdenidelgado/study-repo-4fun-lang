spring:
  datasource:
    dbcp2:
      driver-class-name: com.mysql.cj.jdbc.Driver
      host: jdbc:mysql://172.17.0.2:3306/testdb?useTimezone=true&serverTimezone=UTC
      username: root
      password: root
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: false